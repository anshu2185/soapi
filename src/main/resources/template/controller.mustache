package {{{basePackage}}}.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;


/*
 *  Example-Controller:
 *  This Class would be responsible for Mapping from Request to internal Datamodel (and backwards),
 *  for calling Backend-Services and handling Backend-Exceptions
 *  So it decouples the WSDL-generated Classes from the internal Classes - for when the former changes,
 *  nothing or only the mapping has to be changed
 */ 
@Component
public class {{{wsdlServiceName}}}ServiceController {

    private static final Logger LOG = LoggerFactory.getLogger({{{wsdlServiceName}}}ServiceController.class);

{{#each wsdlproperties}}
    @Override
    public {{returnType}} {{#firstLetterSmallop}}{{operationName}}{{/firstLetterSmallop}}({{inputParam}} {{#firstLetterSmallInput}}{{inputParam}}{{/firstLetterSmallInput}}) throws {BusinessException {}

   {{/each}}
}
